# GitHub Stars Daily Tracker

自动追踪和整理 GitHub 上 star 飙升项目的自动化工具，每天早上 7 点自动更新并部署到 GitHub Pages。

## 功能特点

- ⏰ 每天自动运行（UTC 时间早上 7 点）
- 📊 获取过去 24 小时内 star 增长最多的前 10 个项目
- 📝 生成详细的项目信息文档
- 🎨 自动部署到 GitHub Pages，提供美观的网页展示
- 📈 包含统计数据和趋势分析

## 技术栈

- **Node.js** - 运行时环境
- **GitHub Actions** - 自动化 CI/CD
- **GitHub REST API** - 获取项目数据
- **GitHub Pages** - 静态网站托管

## 项目结构

```
.
├── .github/
│   └── workflows/
│       └── daily-stars.yml      # GitHub Actions 工作流配置
├── src/
│   ├── fetch-trending.js        # 获取热门项目脚本
│   ├── generate-markdown.js     # 生成文档脚本
│   └── deploy-pages.js         # 部署脚本
├── docs/                       # 生成的文档（部署到 GitHub Pages）
├── data/                       # 数据文件
├── package.json
└── README.md
```

## 本地开发

### 安装依赖

```bash
npm install
```

### 运行脚本

获取热门项目数据：

```bash
npm run fetch
```

生成文档：

```bash
npm run generate
```

部署到 GitHub Pages（需要在 GitHub Actions 环境中运行）：

```bash
npm run deploy
```

一键执行所有步骤：

```bash
npm start
```

## GitHub Actions 工作流

工作流文件位于 `.github/workflows/daily-stars.yml`，包含以下步骤：

1. **定时触发**：每天 UTC 时间早上 7 点自动运行
2. **手动触发**：支持通过 GitHub Actions 界面手动触发
3. **构建阶段**：
   - 检出代码
   - 设置 Node.js 环境
   - 安装依赖
   - 获取热门项目数据
   - 生成文档
   - 上传构建产物
4. **部署阶段**：
   - 部署到 GitHub Pages

## 部署到 GitHub Pages

### 1. 启用 GitHub Pages

1. 进入仓库的 **Settings** 页面
2. 在左侧菜单中找到 **Pages**
3. 在 **Source** 部分选择 **GitHub Actions**
4. 保存设置

### 2. 配置 GitHub Actions 权限

1. 进入仓库的 **Settings** → **Actions** → **General**
2. 在 **Workflow permissions** 部分：
   - 选择 **Read and write permissions**
   - 勾选 **Allow GitHub Actions to create and approve pull requests**
3. 点击 **Save** 保存

### 3. 配置 Secrets（可选）

如果你需要使用自定义的 GitHub Token 来提高 API 限制：

1. 进入仓库的 **Settings** → **Secrets and variables** → **Actions**
2. 点击 **New repository secret**
3. Name: `GITHUB_TOKEN`
4. Value: 你的 Personal Access Token（需要 `repo` 权限）
5. 点击 **Add secret**

## 生成的文档内容

### Markdown 文档

生成的 Markdown 文件包含：

- 项目总览表格
- 前 10 名热门项目详细列表
- 每个项目的完整信息：
  - 项目名称和链接
  - Star 数、Watchers、Forks 数
  - 编程语言
  - 作者信息
  - 创建和更新时间
  - 项目标签（Topics）
  - README 摘要
- 统计信息汇总

### HTML 网页

生成的 HTML 页面提供：

- 响应式设计，支持移动端
- 渐变色背景和卡片式布局
- 交互式表格和项目卡片
- 统计数据可视化
- 优美的排版和样式

## 定制化

### 修改运行时间

编辑 `.github/workflows/daily-stars.yml`：

```yaml
on:
  schedule:
    - cron: '0 7 * * *'  # 修改这里的 cron 表达式
```

### 修改项目数量

编辑 `src/fetch-trending.js`：

```javascript
const REPO_COUNT = 10;  // 修改这里获取的项目数量
```

### 修改时间范围

编辑 `src/fetch-trending.js` 中的日期计算逻辑：

```javascript
const yesterday = new Date();
yesterday.setDate(yesterday.getDate() - 1);  // 修改这里的数值
```

## 注意事项

1. **GitHub API 限制**：未认证的请求有较低的限制，建议配置 Personal Access Token
2. **时区设置**：工作流使用 UTC 时间，如需其他时区请调整 cron 表达式
3. **首次运行**：首次部署可能需要几分钟时间完成

## 故障排除

### 工作流失败

- 检查 GitHub Actions 日志
- 确认 GitHub Pages 权限已正确配置
- 验证依赖是否正确安装

### 数据获取失败

- 检查 GitHub API 限制
- 验证 GITHUB_TOKEN 是否有效
- 确认网络连接正常

### 部署失败

- 确认 GitHub Pages 已启用
- 检查部署目录是否正确
- 验证 GitHub Actions 权限设置

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

---

由 GitHub Actions 自动生成 🤖
